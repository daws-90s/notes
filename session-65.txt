TopologySpreadConstraint PodDisruptionBudget

spread pods across zones and nodes, without TopologySpreadConstraint scheduler may schedule the multiple pod replicas in the same node

maxSkew is the allowed no of different count between the balanced and inbalanced nodes
topologyKey -> Zone or node
whenUnsatisfiable -> ScheduleAnyWay/DontSchedule

TopologySpreadConstraint gaurentees high availability

PodDisruptionBudget
voluntary situations like draining(upgrade) autoscaling prevents pod evictions if PodDisruptionBudget does not satisfy with minAvailability, we can always set minAvailability based on number of nodes. PodDisruptionBudget will not protect in in-voluntary situations like crashing and deleting the node

Network
========
VPC-CNI is the plugin inside EKS for networking, pod gets IP from VPC CIDR pool. pods are first class citizens, they get real IP we can reach pods directly from VPC network. number of pod IPS are based on capacity and instance type of EKS nodes

ENI, How many pods per interface

namespace is a isolated project space in k8s. we need to setup network policies

if no policies exist in a namespace, then all ingress and egress traffic is allowed to and from pods in that namespace.

aws eks update-addon \
    --cluster-name roboshop-dev \
    --addon-name vpc-cni \
    --configuration-values '{"enableNetworkPolicy":"true"}'
	
aws eks describe-addon \
    --cluster-name roboshop-dev \
    --addon-name vpc-cni
	
Kubernetes Architecture
=======================
Master and Node

Control Plane
=============
1. api-server -> all requests to k8s hit api-server first, it validates authentication and authorisation. if success it will then call other components like scheduler
2. scheduler -> schedules pods on nodes. it checks node resources, node selectors, taints and tolerations, affinity, TopologySpreadConstraint, etc. 
3. control manager
	Node controller: runs desired number of nodes all the time.
	EndpointSlice controller: responsible to attach pods IPs as endpoints to services
	ServiceAccount controller: responsible for creating and managing serviceaccounts
	Job controller: one time tasks resources like jobs
4. etcd. memory to cluster. configmaps, secrets, manifest files are stored here.
5. cloud controll manager -> integrates with cloud provider

Node components
===============
kubelet -> agent running inside node that connects control plane and nodes. it always reports node status. responsible to run pods in nodes

kube-proxy -> manages network rules inside the node. brings traffic to the pods

Container runtime -> containerd is the run time in eks. 

add-ons
======


Namespace
pod
services
sets
	replicaset
	deployment
	statefulset
	daemonset
HPA
PV, PVC, SC
Ingress
Gateway
RBAC
SA
taints and tolerations
selectors
TopologySpreadConstraint
PodDisruptionBudget
helm charts
upgrading cluster
blue green deployments
network policies

git
====
create repo
clone repo
adding files
adding files to staging/temp area; commit to local repo; push to remote repo
pull the changes
add origin

branch
======
master
main -> always pointing to production

we create new branch, we do changes changes and test in all environments

merge vs rebase
===============
1. merge creates one extra commit with 2 parents called as merge commit
2. merge preserves history of changes

rebase
-------
1. rebase will not create extra commit
2. it will not preserve the history, it has linear history like main branch moved ahead alone
3. it will rewrite commits

use merge, if a branch is used by multiple users.
use rebase for private and local branches.

Git conflicts
=============
while merging if git finds diff code in the same line number then git gives conflicts. It is the developers responsibility who developed this code.

eggdosa-suresh
eggdosa-ramesh

Before pushing raising PR, pull the changes and resolve conflicts locally by merging main branch on to local branch.

Branching Strategy
==================
Feature branching strategy or GitHub flow

main/master and feature branch
main/master -> long lived branch
feature branch -> short lived branch

whatever change we want to do, we create feature branch. When develoment is completed we create PR to bring changes to main branch. If we get conflicts we resolve them.

We can do the deployment into DEV, UAT, PROD from main branch. When PROD is success we can tag the commit using our version. v1.4.0

main develop release
feature hotfix

same code across all the environments, configuration can be changed.

Git reset
=========
Reset the code already committed. Undo the local commits. This is only useful for local branches.
1. Soft -> code comes to staging area
2. Mixed -> code comes to workspace
3. Hard -> code will be deleted

git squash == interactive rebase
==========
a feature is a code change from previous commit to present commit
1.0.0
1.1.0 -> Egg Dosa
Egg Dosa = 1.1.0 - 1.0.0 = diff of code

We can combine multiple commits into single commit using squash. But don't do this for shared branches, do this only for private branches.

git rebase -i d4c10f5

pick 8bf896 (HEAD -> masala-dosa, origin/masala-dosa) Aloo masala added
squash 538e532 Oil added
squash 3972056 Dosa Batter added

give proper commit message for this squashed commit.

Git Revert
==========
This is used for shared/remote branches. If we want to undo changes which went into shared branches already, we can use revert. This will not undo the commit. This will create extra commit correcting those changes.
History is preserved, nothing is rewritten..

git cherry pick
==============
if the required code is already developed, then we can pick that code using cherry pick, we will get new code added using that commit.

C1
C2 -> extra code added as part of this commit

git cherry-pick C2

git stash








